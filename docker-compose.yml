services:
  alphashield-backend:
    build: .
    container_name: alphashield_ai_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/Backend
    command: uvicorn main:app --app-dir Backend --host 0.0.0.0 --port 8000 --reload
    restart: always